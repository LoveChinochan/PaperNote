# 基于时空光子计算的超快动态机器视觉
## 主要成果
提出了一种时空光子计算架构，来实现高度并行的空间计算和高速时间计算，并实现了三维时空平面。

在空间复用系统上看，光子处理速度提高了40倍，参数减少了35倍。波分复用系统实现了动态光场的全光非线性计算，摆脱了存储墙限制。

## 传统局限
光子计算具有低损耗、并行、超快和无时钟光传播的特点，可用于缓解电子计算的局限性并加速光场处理

目前最先进的光子计算无法处理超快动态光场。目前处理时空光场，动态输入在空间域按顺序计算，并且不同时间步长的空间输出需要通过数电设备进行传输、存储和处理。存储器的数字传输和读写操作对推理速度构成了严重的限制，拖慢了速度。

空间和时间光子计算相结合消除数字瓶颈，但空间与时间维度不匹配（二者转换会丢失信息）。空间二维而时间是一维，自由度有限（无法覆盖整个时空平面），导致它们在处理高维动态光场时效率低下。要真正实现高速机器视觉和超快动态分析，必须发展一种能同时在空间和时间维度上计算的时空光子计算架构。

`空间光子计算：利用光在空间维度上的传播和调制来计算。并行度非常高（所有像素同时传播、调制）；速度快（光速级别）；但只能对“单帧静态图像”操作，对随时间变化的动态信号支持不好。`

`时间光子计算：利用光在时间维度上的演化和延迟来计算。能处理随时间变化的信号；适合做动态序列分析；但一般只能处理“一维时间信号”，空间特征（图像结构）会丢失。`

## STPC
一种用于超快动态机器视觉的时空光子计算架构，可以直接在光域里提取时空信息，并根据光场的时间变化来做预测。

### STPC分为三大部分
1.空间计算模块：
就像光学版的“卷积层”或“矩阵乘法器”。
用空间光调制器对输入图像做逐点运算、卷积等 → 提取空间特征。

2.时空复用模块：
SMUX（空间复用）：把不同空间区域的信息映射到不同的时间信号。
WMUX（波长复用）：把不同空间特征编码到不同波长上。
作用：解决“空间 → 时间”维度不匹配问题，保证空间内容在转成时间序列时不丢信息。

3.时间计算模块
有一个光学延迟缓冲器（类似光的“记忆”），可以暂存并合并快速变化的光信号。
在这个时间维度上执行矩阵-向量乘法（MVM），相当于给不同时间点的信号加权、连接。
和空间计算模块配合，就能实现真正的“全时空计算”。

### 优化方法
提出了一个实验系统学习方法：
不仅训练神经网络里的“权重参数”，还会把光学系统的物理参数一起优化。
这样能减少物理误差，让实验系统的准确率提升 80%以上。

### 实验结果
#### SMUX-STPC 网络：

测试了脉冲序列分类（类似动态手写数字）、人体动作识别、物体运动跟踪；

结果显示，比现有光子计算方法更快（推理时间缩短一个数量级以上），而且参数更少（更高效）。

#### WMUX-STPC 网络（双层 + 全光非线性）：

能处理纳秒级帧时间（3.57 ns 一帧） 的超快闪烁序列；

全程光学运算，没有内存读/写延迟；

加入了“多元非线性激活函数”，能解决更复杂的分类问题。
