# Fullyforward modetraining  for optical neural networks 光学神经网络的全前向模式训练
## 主要成果
开发了全前向模式学习的方法，在物理系统上实现计算密集型训练过程，可以使得机器学习操作在物理系统上并行高效完成。

`通过光学性质，如光的传播、干涉、衍射等物理规律，来实现人工神经网络中的运算的物理系统`

本文的百万参数的最深层光学神经网络可以达到与理想模型相当的精度，并且支持通过散射介质进行全光学聚焦，分辨率达到衍射极限；可以超千赫兹的帧速率并行成像视线之外的隐藏的物体，并且可以在室温下以 $5.4 \times 10^{'''}$ 次操作/秒/瓦的能量效率进行全光学处理。

并且证明了FFM学习可以在没有解析模型的情况下自动搜索非厄米奇点。

## 传统局限性
光学人工智能可以实现大带宽和高能效计算，人工智能也可用于光学设计。但二者都是在电子计算机上以硅片形式实现，需要严格的建模和大量的训练数据来提取系统信息；

光学人工智能方法主要涉及ANN的光学仿真，光学架构设计需要在电子计算机上执行，故而校正实验系统误差困难，计算资源需求大。并且精确建模光学系统不可能，即离线模型和真实系统之间总不匹配。

`光学人工智能：1.大规模矩阵乘法：光波在空间中传播、通过透镜或相位调制器时，本质上对应的是线性变换（可用矩阵相乘来描述）；2.非线性激活：光学系统本身是线性的，但可以引入非线性光学材料（比如饱和吸收体、光学晶体、光纤中的非线性效应），在光强超过一定阈值时，响应会“弯曲”，这就可以实现类似 ReLU 的激活函数。`

`离线模型：训练阶段
用数字计算机（GPU/CPU）跑仿真，得到神经网络的最优参数（权重矩阵）。
这些参数再映射到光学硬件，比如调节空间光调制器 (SLM)、相位调制器、光干涉器阵列等，让它们物理上实现这些权重。
在推理阶段，光学系统拿到输入（比如一幅图像对应的光场），经过光学器件传播，输出结果。光学器件此时不再更新参数，整个网络的权重是固定的。`

### 在光学系统上实施机器学习算法的传统局限
扰动法：给某个参数（比如相位调制器上的相位）加一点小扰动，看看输出误差变大还是变小，从而估计梯度方向。需要对每个参数都试一遍，效率很低，尤其是参数数目达到百万级时几乎不可行。

梯度下降反向传播：效率较扰动法更高，但是要求系统严格校准，且光路严格对称。

混合方法：前向传播在光学系统中快速实现，反向传播在计算机里离线完成。但是必须有一个足够准确的光学系统的数值模型，才能在计算机里做反向传播。如果光学系统太复杂或不可建模（比如有噪声、非理想器件、非线性、散射），离线建模就失效了。

## FFM方法
不依赖数值仿真，在光学系统上进行训练。将光学系统当作一个神经网络，里面的光学元件就是网络参数，系统可根据具体任务目标实现自我参数调整。

利用空间对称性和洛伦兹互易性，可仅使用前向传播，得到梯度信息，不需要反向传播。

`空间对称性：光传播的规律在正向和反向是对称的。`

`洛伦兹互易性：输入和输出可以在物理上互换。`

最后再使用梯度下降，实现参数更新。
