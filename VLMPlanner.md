# VLMPlanner：将视觉语言与动作规划相结合
## 主要贡献
将依赖于抽象的感知或基于地图的输入，更改为能够对原始图像进行推理的视觉语言模型，并与基于学习的实施规划器相结合。
使用VLM捕捉视觉信息，利用其推理能力来实时指导规划器生成稳健且安全的轨迹。（就是Asyncdriver上多了个图像的补充输入）

开发了上下文自适应推理门机制，能够基于场景复杂性动态调整推理频率来模仿人类，实现性能和效率的平衡。

构建了两个图像-文本数据集DriveBQA和ReasoningVQA。

在nuPlan上进行评测，有较好的性能。

## 相关工作
结构化输入和BEV地图，受token限制，缺乏细粒度特征的表达能力。

一些方法将LLM集成到实时规划器，避免了LLM直接生成轨迹。
### 端到端自动驾驶
目前已经从仅图像到了多模态融合框架的转变，并进一步发展为模块化的端到端结构，将感知、预测和规划整合到一个统一模型中。但虚拟环境和真实世界有巨大的差距，测试阶段仍面临着真实性和多样性的挑战。
### 基于学习的自动驾驶
规划器主要集中在基于学习的方法，可分为模仿学习方法和离线强化学习方法。旨在学习大规模数据集来模仿人类驾驶行为。

GameFormer：基于DETR的分层轨迹预测框架。

DTPP：引入可微框架，用于自我的条件预测和成本模型的联合训练。

PlanTF：引入基于注意力的状态dropout编码器和增强技术。

以上方法受限于数据集范围和模型复杂度，对于路线信息理解和环境感知仍有较大改进空间。
### 基于LLM的语言增强运动规划
PlanAgent：采用BEV地图，促进场景理解和轨迹规划。

LLM-ASSIST：混合规划器，将LLM与基于规则相结合。也有将LLM与基于规则规划器解耦，LLM仅在计划的轨迹评分未能满足阈值时进行干预。

Asyncdriver：将感知和地图提前预编码输入，解决token限制。

以上方法对于长尾问题和细粒度感知的能力有限。

## 研究方法
VLMPlanner将基于学习的实时规划器与VLM集成。

实时规划器处理地图信息，以快速生成预测轨迹。

VLM接收多视角图像、地图信息、系统消息和导航指令，复合编码输入至VLM，其输出用于指导实时规划器。

CAI-Gate接受多视角图像和地图信息，评估场景复杂度，动态调整推理频率。

### 整体框架
多模态输入：（1）系统消息，（2）导航指令，（3）编码后的地图特征，（4）编码后的图像特征。

[![2025-07-31-111335.png](https://i.postimg.cc/K8kQ3NGL/2025-07-31-111335.png)](https://postimg.cc/mP4707F2)

地图信息：包含自车历史状态信息、邻车历史状态信息、车道特征、人行横道特征。以自车为中心的坐标系，张量中缺失位置使用0填充。采用Gameformer中的MapEncoder来编码，使用MAPAdapter（MLP）来匹配文本token嵌入的维度，获取地图特征。

多视角图像：提取当前时间步的图像，使用CLIP编码，获得视觉特征。将图像特征和对应的位置编码输入到Q-Former的3D感知模块中，以结合空间感知。在3D感知模块中，初始化一组均匀分布可学习的参考点，通过MLP网络为每个点生成3D坐标，以产生查询。使用交叉注意力，将图像特征投影至3D空间，减少输入到VLM的token数量。最后使用ImgAdapter将特征维度与VLM要求对齐，生成处理后的图像特征。

导航指令：训练阶段，直接来源于未来轨迹的真实值（文本形式描述）；推理阶段，基于车道信息和来自地图的导航数据生成。

VLM引导的实时规划器：将地图、图像、语言嵌入输入VLM，获取隐藏层特征。采用Asyncdriver的自适应注入块，将VLM理解的场景信息注入到实施规划器中，生成规划结果。

$s^{l+1} = g \cdot MultiHeadAttn(Q, K_h, V_h) + MultiHeadAttn(Q, K_{s^l}, V_{s^l})$

即提取最后一个token的隐藏特征h_n，并通过特征适配器对其进行投影得到h。做一个交叉注意力，乘以一个自适应门的权重，加上上一层编码器的交叉注意力结果。

### 上下文自适应推理门
基于学习的门控：采用实时多视角图像输入，利用EfficientNet-B0的网络快速评估驾驶场景的复杂性。利用多视角图像与语言提示配对，使用Gemini对场景复杂性级别进行分类，从而得到一个图像真值对的数据集，70%用于训练，30%用于测试。训练一个序数回归模型来对场景复杂性级别进行分类，然后确定VLM的适当推理频率。

基于规则的门控：驾驶场景的复杂性由几个关键因素决定：周围车辆和行人的数量和距离、自我车辆位置的车道数量、自我车辆的当前速度和加速度，以及其与交叉路口或转弯处的距离。通过单独评估和汇总这些指标，最终得出一个综合的场景复杂性评级。

### 数据集构建
DriveVQA：专门设计用于增强视觉语言模型对自动驾驶指令、轨迹和控制信号的理解。

(1) 从数据集中提取自车状态信息、历史/未来轨迹以及多视角图像；(2) 算法式生成相应的文本描述；(3) 通过随机成对排列系统地组合自动驾驶指令、轨迹数据和控制信号，然后将它们与多视角图像集成，形成全面的视觉-文本问答对。

ReasoningVQA：专门设计用于增强视觉语言模型在驾驶场景中的场景理解和推理能力。

(1) 将原始数据集中的原始地图数据转换为全面的文本描述，包括精确的位置数据、自车和周围车辆的历史轨迹信息以及详细的车道规格；(2) 集成所有相关信息；(3) 提示 GPT-4 生成上下文相关的规划决策，并附有详细的推理过程，严格遵守交通规则和提供的地图信息。

### 训练
包含预训练阶段和微调阶段。

#### 预训练阶段
使用DriveVQA和ReasoningVQA进行两轮VLM预训练。第一轮仅使用DriveVQA数据，以增强VLM对自动驾驶指令、轨迹和控制信号的理解。第二轮使用ReasoningVQA数据和少量DriveVQA数据，以提高VLM对自动驾驶场景和交通规则的理解，以及其推理能力。两轮训练都采用LoRA。

#### 微调阶段
使用来自实时规划器的预训练权重初始化模型。总训练损失包括两个组成部分：（1）规划器固有的规划损失 $L_{plan}$ ;
以及（2）遵循AsyncDriver，采用辅助任务，包括自车速度/加速度预测、速度决策预测、交通灯状态预测、相邻车道存在预测和变道预测。所有辅助任务的组合损失表示为 $L_{aux}$ 。

Gameformer损失：

对邻近智能体行为的概率性、多模态预测：使用高斯混合模型（GMM）来捕捉其他智能体未来轨迹的多种可能性和不确定性，并通过负对数似然损失来优化这些预测。这使得模型能够更好地理解和应对环境中的不确定性。

对自车轨迹的精确性预测：直接使用L1 损失来确保自车规划的路径与真实、理想的路径尽可能接近。

PlanTF损失：

轨迹 L1 损失：让模型预测出接近正确的自车轨迹。

交叉熵损失：让模型对它预测的最佳自车轨迹有更高的置信度。

周围物体轨迹 L1 损失：让模型能够准确预测其他智能体的行为，从而为自车规划提供更安全的上下文信息。


## 实验
对比了基于规则、基于学习和基于 LLM 的多种现有方法，包括 PDM-Closed、GameFormer、PlanTF、AsyncDriver等。

### 结果
Open-Hard20 结果：VLMPlanner 的两个版本都比各自的Baseline规划器（GameFormer 和 PlanTF）有显著提升，并且优于当前最先进的开环方法 PlanTF。作者特意提了一嘴，AsyncDriver表现反而不如原始 GameFormer。

Close-Hard20 结果：VLMPlanner 在反应式和非反应式闭环配置下均达到了最先进的性能，尤其在反应式条件下比 GameFormer 提升了 3.33% 的分数。

安全性提升：VLMPlanner 显著降低了自车的碰撞概率，这归因于 VLM 集成后实时规划器对场景（尤其是自车周围区域）理解能力的增强。

定性结果：通过具体案例（如“等待行人”和“红灯停车”）的视觉对比，VLMPlanner 展现出比 AsyncDriver 更准确的场景感知和决策能力，有效避免了潜在事故。

### 消融实验
上下文自适应推理门控 (CAI-Gate)：该模块根据场景复杂性（分为五个级别）动态调整 VLM 的推理频率。实验表明，CAI-Gate 即使在降低 VLM 推理频率的情况下，也能保持与固定间隔推理（如 AsyncDriver）相当甚至更好的性能，证明了其在节约计算资源的同时保持性能的能力。

预训练：通过在 DriveVQA 和 ReasoningVQA 数据集上进行预训练，VLMPlanner 在闭环测试中取得了显著的性能提升。这表明预训练能有效增强 VLM 对自动驾驶场景中车道结构、车辆/行人交互和交通规则的理解和推理能力。
